# =============================================================================
# Dependencias del proyecto — instalar todo:  pip install -r requirements.txt
# =============================================================================

# --- LLM & Agentes (Fase 2) ---
langchain>=0.3.0
langchain-core>=0.3.0
langgraph>=0.2.0

# --- Proveedores de LLM (failover: LM Studio local -> Groq) ---
langchain-openai>=0.2.0         # LM Studio (servidor local OpenAI-compatible, PRIMARIO)
langchain-groq>=0.2.0           # Groq (fallback cloud, gratis y rápido)
langchain-huggingface>=0.1.0    # Embeddings del RAG (NO se usa como LLM)

# --- RAG: guías clínicas PDF (Fase 3). Ojo: sentence-transformers trae torch (~2 GB) ---
langchain-chroma>=0.2.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
sentence-transformers>=3.0.0
pypdf>=4.0.0

# --- Observabilidad ---
langsmith>=0.1.0                 # Tracing de los agentes (opcional, por env vars)

# --- Utilidades ---
python-dotenv>=1.0.0
requests>=2.31.0

# --- UI (Fase 7) ---
chainlit>=1.0.0
